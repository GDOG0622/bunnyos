// 美化教程"一键复制"用的默认 CSS 底稿。S38 会从 styles.css 抽取真实规则填进来。
// 现阶段（M2 骨架）留空字符串占位，不影响商城页加载。详见 §4。
const DEFAULT_SKIN_CSS = '';
const DEFAULT_FRAME_CSS = '';
const DEFAULT_BUBBLE_USER_CSS = '';
const DEFAULT_BUBBLE_CHAR_CSS = '';
const DEFAULT_AVATAR_HINT = ''; // 头像走图片直链，不用 CSS
const DEFAULT_BG_HINT = '';     // 背景图走图片直链，不用 CSS
